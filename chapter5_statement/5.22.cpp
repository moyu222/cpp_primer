#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    while (int sz = get_value()) {
        if (sz <= 0) continue;
    }

    int sz;
    do {
        sz = get_value();
    } while (sz <=0);
}

int get_value() {
    return 2;
}